#include<iostream>

using namespace std;

int main()
{
	int n;
	int len = 0;
	string s;

	cin >>n;

	for (int i = 0;i < n; i++)
	{
		cin >> s;
		int len = s.length();
		cout << s[0] << s[len - 1] << "\n";
	}

	return 0;
}
